[package]
name = "rustc-weight-attribute-fixture"
version = "0.1.0"
edition = "2021"
description = "Test fixture for compiler-backed pallet weight attribute checks"
publish = false

[workspace]

[dependencies]
pallet = { package = "rustc-weight-attribute-pallet", path = "pallet" }
